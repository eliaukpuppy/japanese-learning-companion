import { motion } from "framer-motion";
import { CheckCircle2, ChevronRight } from "lucide-react";
export default function CourseTab({stages,currentStageId,progressOfStage,setCurrentStage,toggleUnit}){return <motion.main initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="space-y-5">{stages.map((stage)=>{const progress=progressOfStage(stage);const isCurrent=stage.id===currentStageId;return <section key={stage.id} className={`rounded-[2rem] bg-white/80 p-5 shadow-sm ring-1 ${isCurrent?"ring-rose-300":"ring-rose-100"}`}><div className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div><div className="mb-2 flex flex-wrap items-center gap-2"><h2 className="text-xl font-bold text-stone-900">{stage.name}</h2>{isCurrent&&<span className="rounded-full bg-rose-500 px-3 py-1 text-xs font-bold text-white">进行中</span>}<span className="rounded-full bg-stone-100 px-3 py-1 text-xs font-medium text-stone-600">{stage.duration}</span></div><p className="max-w-3xl text-sm leading-6 text-stone-600">{stage.goal}</p></div><button onClick={()=>setCurrentStage(stage.id)} className="inline-flex items-center justify-center gap-2 rounded-2xl bg-stone-900 px-4 py-2 text-sm font-bold text-white hover:bg-stone-700">设为当前阶段 <ChevronRight size={16}/></button></div><div className="mt-4 h-2 rounded-full bg-rose-50"><div className="h-2 rounded-full bg-rose-400 transition-all" style={{width:`${progress}%`}}/></div><div className="mt-5 grid gap-3 lg:grid-cols-2">{stage.units.map((unit)=><button key={unit.id} onClick={()=>toggleUnit(stage.id,unit.id)} className={`flex items-center gap-3 rounded-3xl border p-4 text-left transition ${unit.done?"border-emerald-100 bg-emerald-50":"border-rose-100 bg-white hover:bg-rose-50"}`}><CheckCircle2 className={unit.done?"text-emerald-500":"text-stone-300"} size={22}/><div className="flex-1"><div className="font-semibold text-stone-900">{unit.title}</div><div className="mt-1 text-xs text-stone-500">预计：{unit.time}</div></div></button>)}</div></section>})}</motion.main>}
